tasks:
  Run all tasks:
    shell: python -m robot --report NONE --outputdir output --logtitle "Task log" tasks.robot

condaConfigFile: conda.yaml
artifactsDir: output
# screenshotDir:  output/screenshot
# pdfDir: output/receipts
PATH:
  - .
# PYTHONPATH:
#   - ./usr/local/opt/python@3.10/bin/python3
ignoreFiles:
  - .gitignore
